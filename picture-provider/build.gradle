apply from: '../androidModule.gradle'

dependencies {
    api "androidx.exifinterface:exifinterface:$androidxVersion"
}

gradle.ext.configAndroidStandardDependencies(this, [
        'core-app',
        'core-ui',
        'logger',
        'rx-extension'
])