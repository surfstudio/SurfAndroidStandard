import ru.surfstudio.android.build.DependencyConfigurator

apply from: '../../buildSrc/androidSample.gradle'

android {
    defaultConfig {
        applicationId "ru.surfstudio.android.broadcast.extension.sample"
    }
}

dependencies {
    DependencyConfigurator.projectImplementation(project, "broadcast-extension")
    DependencyConfigurator.projectImplementation(project, "picture-provider")

    DependencyConfigurator.kapt(project, "com.google.dagger:dagger-compiler")
}
