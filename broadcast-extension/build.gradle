apply from: '../androidModule.gradle'

uploadArchives.dependsOn(':logger:uploadArchives')

dependencies {
    compileOnly project(':logger')

    implementation "io.reactivex.rxjava2:rxjava:$rxJava2Version"
}

gradle.ext."$name" = ["logger"]