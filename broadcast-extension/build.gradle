apply from: '../androidModule.gradle'

dependencies {
    debugApi project(':logger')
    releaseApi "ru.surfstudio.android:logger:$moduleVersionName"

    implementation "io.reactivex.rxjava2:rxjava:$rxJava2Version"
}