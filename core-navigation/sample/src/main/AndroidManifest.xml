<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.surfstudio.android.core.navigation.sample">

    <application
        android:name="ru.surfstudio.android.sample.dagger.app.DefaultApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="ru.surfstudio.android.core.navigation.sample.ui.screen.main.MainActivityView"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ru.surfstudio.android.core.navigation.sample.ui.screen.profile.ProfileActivityView" />
    </application>
</manifest>
