package ru.surfstudio.android.security.session

/**
 * Интерфейс-маркер, означающий, что класс не требует активной сессии.
 * Используется на экранах, где нет необходимости сбрасывать сессию.
 */
interface SessionFree