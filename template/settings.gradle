apply from: 'config.gradle'

include "$templatePrefix:base-ui",
        "$templatePrefix:base",
        "$templatePrefix:i-initialization",
        "$templatePrefix:i-network",
        "$templatePrefix:i-session",
        "$templatePrefix:i-token",
        "$templatePrefix:app-injector",
        "$templatePrefix:domain",
        "$templatePrefix:i-auth",
        "$templatePrefix:f-auth",
        "$templatePrefix:f-main",
        "$templatePrefix:f-debug",
        "$templatePrefix:f-splash",
        "$templatePrefix:f-debug-no-op",
        "$templatePrefix:test-utils"

apply from: 'android-standard/androidStandardSettings.gradle'
apply from: 'buildCache.gradle'