apply from: '../config.gradle'

dependencies {
    gradle.ext.androidStandardModules.each {
        api "$surfCoreUrl:$it:$surfCoreVersionName"
    }
}

apply from: '../scripts/androidStandardDebugDependencies.gradle'