apply from: '../androidSample.gradle'

android {
    defaultConfig {
        applicationId "ru.surfstudio.android.location.sample"
    }
    defaultConfig {
        testInstrumentationRunner "ru.surfstudio.android.location.sample.LocationSampleTestRunner"
    }
}

dependencies {
    implementation project(':location')
    implementation project(':sample-dagger')

    kapt "com.google.dagger:dagger-compiler:$daggerVersion"
}