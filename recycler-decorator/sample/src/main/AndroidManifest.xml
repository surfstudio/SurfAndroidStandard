<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.surfstudio.android.recycler.decorator.sample" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="ru.surfstudio.android.recycler.decorator.sample.MainActivityView"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".list.LinearDecoratorActivityView"
            android:screenOrientation="portrait"/>

        <activity android:name=".pager.CarouselDecoratorActivityView"
            android:screenOrientation="portrait"/>

        <activity android:name=".easyadapter.simple.EasyAdapterDecoratorActivityView"
            android:screenOrientation="portrait" />

        <activity android:name="ru.surfstudio.android.recycler.decorator.sample.easyadapter.chat.ChatActivityView"
            android:screenOrientation="portrait" />
    </application>

</manifest>