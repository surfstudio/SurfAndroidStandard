<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="CustomViewTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SampleTitleSubtitleViewStyle">
        <item name="titleText">Заголовок</item>
        <item name="titleTextAppearance">@style/Text.Light.12</item>
        <item name="subTitleText">Подзаголовок</item>
        <item name="subtitleTextAppearance">@style/Text.Dark.16</item>
        <item name="titleDrawablePadding">4dp</item>
        <item name="subTitleDrawablePadding">4dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- Кнопка без бэкграунда, но с эффектами риппл или
    полупрозрачным бэкграундом на более ранних версиях. -->
    <style name="Button.Borderless" parent="Widget.AppCompat.Button.Borderless" />

    <style name="DefaultButton" parent="Button.Borderless">
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
    </style>

    <style name="DefaultButton.Primary">
        <item name="android:textAppearance">@style/Text.White.16</item>
        <item name="android:background">@drawable/bg_button_primary</item>
    </style>
</resources>