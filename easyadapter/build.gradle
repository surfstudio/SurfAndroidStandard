apply from: '../androidModule.gradle'

dependencies {
    compileOnly "com.android.support:support-annotations:${appcompatVersion}"
    compileOnly "com.android.support:recyclerview-v7:${appcompatVersion}"
}
