<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/default_margin">

    <Button
        android:id="@+id/check_root_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/default_margin"
        android:text="@string/check_root_btn_text" />

    <Button
        android:id="@+id/open_session_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/default_margin"
        android:layout_below="@id/check_root_btn"
        android:text="@string/open_session_btn_text"/>

    <EditText
        android:id="@+id/enter_pin_et"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="numberPassword"
        android:layout_toStartOf="@id/submit_pin_btn"
        android:layout_below="@id/open_session_btn"
        tools:ignore="LabelFor" />

    <Button
        android:id="@+id/submit_pin_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/open_session_btn"
        android:layout_alignParentEnd="true"
        android:text="@string/submit_pin_btn_text" />
</RelativeLayout>