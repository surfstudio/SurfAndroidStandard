apply plugin: 'maven'
apply from: '../config.gradle'

uploadArchives {
    println(System.getenv('surf_maven_username'))
    repositories.mavenDeployer {
        repository(url: "${surf_maven_libs_url}") {
            authentication(
                    userName: System.getenv('surf_maven_username'),
                    password: System.getenv('surf_maven_password'))
        }

        pom.groupId = 'ru.surfstudio.standard'
        pom.artifactId = project.name
        pom.version = moduleVersionName
    }
}