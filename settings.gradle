import ru.surfstudio.android.build.InitializatorKt

InitializatorKt.getModulesName().forEach { module ->
    include ':' + module.first
    project(':' + module.first).projectDir = new File(module.second)
}