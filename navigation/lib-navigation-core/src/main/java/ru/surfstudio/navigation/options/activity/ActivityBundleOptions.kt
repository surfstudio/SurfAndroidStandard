package ru.surfstudio.navigation.options.activity

import android.os.Bundle
import ru.surfstudio.navigation.options.Options

/**
 * Options that are inherited from [ActivityOptions]
 *
 * @param activityOptions [ActivityOptions] placed to a bundle
 */
class ActivityBundleOptions(val activityOptions: Bundle) : Options