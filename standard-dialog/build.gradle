apply from: '../androidModule.gradle'

dependencies {
    compileOnly project(':core-ui')
    compileOnly project(':mvp-dialog')
    compileOnly project(':dagger-scope')
}

gradle.ext."$name" = ['core-ui', 'mvp-dialog', 'dagger-scope']